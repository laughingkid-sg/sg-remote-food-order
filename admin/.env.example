# Supabase — copy to .env and fill in from Project Settings → API.
# Vite exposes only VITE_-prefixed vars to the client. The anon key is a public
# client key; writes are gated by Supabase Auth + RLS, never by hiding this key.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
