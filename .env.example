# Supabase — copy this file to .env.local and fill in from your project's API settings.
# Until these are set, the site builds from local seed data (data/seed.ts).
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
